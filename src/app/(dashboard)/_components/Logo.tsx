import { GraduationCap } from "lucide-react";

const Logo = () => {
  return (
    <div className="ml-4 flex items-center justify-center gap-2">
      <GraduationCap className="text-xl font-semibold" size={36} />
      <h1 className="text-xl font-semibold ">LMS</h1>
    </div>
  );
};

export default Logo;
